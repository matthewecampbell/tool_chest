<div  class="tool_info">
<% @tools.each do |tool| %>
<h3>  <%= tool.name %> - <%= tool.quantity %> - <%= tool.price %> </h3>
  <%= link_to "Edit Tool", edit_tool_path(tool.id) %>
  <%= link_to "See Tool", tool_path(tool.id) %>
</br>
</br>
<% end %>

<%= link_to "Create New Tool", new_tool_path %>
</div>
